# MayaNodesAttributesTest reference outputs

These `.txt` files are golden reference outputs used by the Maya Hydra tests
to compare serialized data sources for primvars and related schema data.

Do not edit their contents or add inline comments unless the corresponding
test expectations are updated, as comparisons are done by exact text match
(with whitespace normalization).
